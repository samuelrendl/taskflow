import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover";

const AssignTaskTo = () => {
  return (
    <Popover>
      <PopoverTrigger>Assign task to</PopoverTrigger>
      <PopoverContent>
        
      </PopoverContent>
    </Popover>
  );
};

export default AssignTaskTo;
